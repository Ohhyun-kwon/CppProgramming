## Q1. C++에서 생성자를 통하여 멤버변수의 초기화를 하는 이유를 설명하라.
A1. 생성자는 객체가 만들어지는 순간 자동으로 호출되므로 멤버변수 초기화를 보장할 수 있음.

## Q2. 생성자 작성시 생성자 몸체에서 멤버를 초기화하는 것 보다 멤버 초기화 리스트에서 초기화하는 이유를 설명하라.
A2. 멤버 초기화 리스트에서 초기화 할 경우 객체가 생성됨과 동시에 멤버변수가 초기화할 때 해당 값으로 초기화되지만 몸체에서 초기화할 경우 대입 연산으로 하기에 비효율.

## Q3. 위임, 타겟 생성자로 나누어 작성하는 이유를 설명하라.
A3. 생성자가 여러개 존재할 때 중복되는 경우를 막을 수 있음

## Q4. 함수 몸체(body)에서 생성자를 직접 호출하면 어떻게 되는지 조사해보라.
A4. 임시 객체를 생성했다가 바로 소멸한다.


<br>
<br>

# 실행 결과  
