## Q1. 객체의 생성과 소멸의 의미를 설명하라.
A1. 프로그램에서 객체를 사용하기 위해 메모리에 할당하는 것을 객체의 생성이라하며, 할당된 객체를 더 이상 사용하지 않을 때 메모리에 접근을 차단하는 것을 객체의 소멸이라고 한다.

## Q2. 지역변수(지역객체), 전역변수(전역객체)의 차이를 조사하라, 선언위치, 접근범위, 생성시점, 소멸시점, 초기화 여부 등의 관점에서 비교하라.
A2. 지역변수는 함수 내에서 선언되며 이에 따라 해당 함수 내에서만 접근할 수 있다. 또한 해당 함수가 호출될 때 변수 또는 객체가 생성하고 함수가 종료되는 시점에 소멸한다. 지역변수의 초기화는 자동으로 되지않아 직접 명시하지 않을 경우 쓰레기값을 갖는다. 전연변수의 경우 함수 바깥에서 선언되며, 모든 함수에서 접근할 수 있다. main 함수가 호출되기전 순차적으로 변수 또는 객체가 생성되며, main 함수가 종료될 경우 소멸한다. 전역변수의 초기화는 0 또는 이외의 값을 운영체제에서 자동으로 초기화해준다.

## Q3. 프로그램 실행 후 main함수가 실행되기 전에 어떤 일이 처리되는지 조사해보라.
A3. 전역변수 또는 전역객체가 순차적으로 생성된다용하기 위해 메모리에 할당하는 것을 객체의 생성이라하며, 할당된 객체를 더 이상 사용하지 않을 때 메모리에 접근을 차단하는 것을 객체의 소멸이라고 한다.

## Q2. 지역변수(지역객체), 전역변수(전역객체)의 차이를 조사하라, 선언위치, 접근범위, 생성시점, 소멸시점, 초기화 여부 등의 관점에서 비교하라.
A2. 지역변수는 함수 내에서 선언되며 이에 따라 해당 함수 내에서만 접근할 수 있다. 또한 해당 함수가 호출될 때 변수 또는 객체가 생성하고 함수가 종료되는 시점에 소멸한다. 지역변수의 초기화는 자동으로 되지않아 직접 명시하지 않을 경우 쓰레기값을 갖는다. 전연변수의 경우 함수 바깥에서 선언되며, 모든 함수에서 접근할 수 있다. main 함수가 호출되기전 순차적으로 변수 또는 객체가 생성되며, main 함수가 종료될 경우 소멸한다. 전역변수의 초기화는 0 또는 이외의 값을 운영체제에서 자동으로 초기화해준다.

## Q3. 프로그램 실행 후 main함수가 실행되기 전에 어떤 일이 처리되는지 조사해보라.
A3. 전역변수 또는 전역객체가 순차적으로 생성된다. 이외에도 운영체제가 프로그램을 메모리에 적재하거나 코드와 데이터의 영역을 분리하는 등의 작업을 수행한다.

## Q4. main함수가 종료 후 프로그램이 종료하기 전에 어떤 일이 처리되는지 조사하라.
A4. 전역변수 또는 전역객체가 역순으로 소멸된다. 



<br>
<br>

# 실행 결과 
<img width="395" height="187" alt="image" src="https://github.com/user-attachments/assets/8325b27e-eb62-488a-8b49-16928a02d181" />


